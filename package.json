{
  "name": "tfk-generate-archive-title",
  "version": "1.1.2",
  "description": "Generate title for archive",
  "license": "MIT",
  "author": {
    "name": "Geir Gåsodden",
    "email": "geir.gasodden@pythonia.no",
    "url": "https://github.com/zrrrzzt"
  },
  "main": "index.js",
  "engines": {
    "node": ">=10.5.0"
  },
  "scripts": {
    "test": "standard && npm audit && ava",
    "test-offline": "standard && ava",
    "coverage": "nyc ava",
    "coveralls": "nyc ava && nyc report --reporter=lcov && cat coverage/lcov.info | coveralls",
    "standard-fix": "standard --fix",
    "refresh": "rm -rf node_modules && rm package-lock.json && npm install"
  },
  "keywords": [
    "tfk",
    "Telemark fylkeskommune",
    "Title",
    "Archive",
    "Generator"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/telemark/tfk-generate-archive-title.git"
  },
  "bugs": {
    "url": "https://github.com/telemark/tfk-generate-archive-title/issues"
  },
  "homepage": "https://github.com/telemark/tfk-generate-archive-title#readme",
  "devDependencies": {
    "ava": "2.4.0",
    "coveralls": "3.0.9",
    "nyc": "15.0.0",
    "standard": "12.0.1"
  },
  "dependencies": {
    "get-skole-aar": "2.0.0"
  }
}
